# HeartBond App Support Site

Support site for **HeartBond** (iOS and macOS).

## Development

This is an Angular project.

- `npm install` to install dependencies.
- `npm start` for a local dev server.
- `npm run build` to build for production.

## Deployment

The site is deployed to GitHub Pages at `heartbond.ikiapps.com`.
